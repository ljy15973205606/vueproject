

## 实现路由切换动画效果
  + 使用vue提供的transition元素把<roter-view></router-view>元素包裹起来；
  + 使用自定义类名实现过渡效果；
  + 设置即将进入的组件的位置为100%；
  + 设置离开的组件的位置为-100%
  + 为防止页面抖动，应该为离开的组件设置position:absolute;
  + 组件切换，屏幕宽度会变为原来的两倍，防止页面滚动，应该为根组件设置overflow：hidden;

## 完成新闻资讯列表的页面配置
  + 创造路由组件
  + 改造路由链接
  + 添加路由对应规则

## 绘制新闻资讯界面的页面效果
 + 使用mui中的media-list.html
 + 使用display：flex，实现弹性盒模型布局，让元素左右贴边，justify-content：space-between；

## 展示新闻列表的数据
 + 为使页面加载完毕就能获取数据，在生命周期钩子函数中调用获取数据的方法（created）;
 + 在methods中定义获取数据的方法；
 + 为简便获取数据，使用了async和await，es7特性，不需要调用.then();
 + 如果要循环渲染数据，需要把数据挂载到data上面；
## 定义全局的时间过滤器来格式化时间
 + 整个项目中，许多组件都需要格式化时间，所以推荐全局过滤器；
 + 定义过滤器；
 + 调用过滤器；
 + 借用第三方插件moment来格式化时间；
## 以模块化的思想抽离main.js中的代码
 + 抽离那些与vm实列关联性不强的代码；
## 实现新闻列表到新闻详情页面的跳转
 + 创建路由组件
 + 改造路由链接
 + 添加相应的路由规则
## 新闻详情页面数据渲染
 + 获取数据
 + 挂载数据到data上
 + 渲染数据
## 评论子组件的封装与引用
 + 创建子组件
 + 在相应父组件的scrip标签中import子组件；
 + 在父组件的component属性中注册子组件；
 + 在父组件的template标签中，以标签名的形式引入子组件；
## 绘制评论组件的样式
## 获取评论内容
 + 获取新闻id,通过属性绑定的形式，父组件向子组件传递id；
 + 根据id和页码，定义获取评论数据的方法，并在钩子created钩子中调用；
 + 把获取到的数据，挂载到data上的cmtlist中，并渲染页面；
## 点击加载更多评论
 + 为点击加载更多按钮，绑定点击事件；
 + 在事件中，让page页码值+1；
 + 然后重新调用获取评论内容的方法(会出现后面请求的数据覆盖之前数据的情况
   ，解决办法是，让前面请求的数据和后面请求的数据使用contact方法拼接起来)
## 实现发表评论功能
 + 点击发表评论按钮，先做非法检查，防止评论内容为空；
 + 校验通过，则调用api接口，把评论提交到后端；
 + 评论成功后，需要在客户端手动组织处一个新的评论对象，并unshift到评论列表数组中；
## 实现图片分享列表中顶部的滑动区域
 + 借助于MUI中的 `tab-top-webview-main.html`样式；
 + 去掉mui-screen类；
 + 页面设置完毕，发现无法滚动，需要手动初始化scroll控件,先导入mui的js文件，产生
   > mui.js:3444 Uncaught TypeError: 'caller', 'callee', and 'arguments' properties may not be accessed on strict mode functions or the arguments objects for calls to them；
  问题产生原因：
  1、webpack打包产生的bundle.js文件默认使用严格模式
  2、mui的js文件中，caller，callee在严格模式下回报错
  3、解决方式是关闭webpack4的严格模式
  + 关闭方法：npm @babel/plugin-transform-modules-commonjs @babel/plugin-transform-strict-mode -D
  + 在.babelrc中 "plugins":[["@babel/plugin-transform-modules-commonjs", { "strictMode": false }]]
  + 在对一些第三方插件进行操作时，在vue的mounted钩子中进行，必须等图片分享组件挂载到页面上。否则调用mui()方法进行初始化是没有任何意义的。
    因为此时页面上没用任何的元素。
  + 移除滑动报错，为mui-slider类添加touch-action:none;
## 解决tabbar区域和图片分类列表js行为冲突问题
 + 原因：每一个tabbar按钮的类样式.mui-tab-item和js行为有冲突；
 + 解决：把所有和`.mui-tab-item`相关的样式属性全部拷贝出来，粘贴在我们新设置的类`.my-tab-item`中，然后进行类名的替换；
## 获取图片分类数据并加载到页面上
 + 获取分类数据；
 + 手动把'全部'分类信息，通过unshift方法添加到数组头部；
 + 把获取到的分类，挂载到data的category中；
 + 默认设置`全部分类`被高亮选中，通过:class结合三元表达式进行使用，把id为0的哪一项设置为默认选中；
## 默认加载全部的分类数据
 + 定义一个getPhotoByCategory方法，接收一个分类id，根据id获取所有的分类列表；
 + 把获取到的图片挂载到data上；
 + 根据mint-UI的lazy-load组件，实现图片的懒加载；
   1. 图片的URL地址需要通过组件的v-lazy来指定；
   2. 同时把图片的样式也粘贴到项目中，同时把image选择器改成img；
## 美化图片列表
## 点击图片分类，切换图片列表数据
 + 为每一个分类，添加点击事件，重新发起数据请求，来获取图片列表；
## 点击图片跳转到图片详情的路由改造
 + 把每个图片的li标签改造成router-link标签，为防止样式改变，添加tag属性为`tag="li"`,to属性采用属性绑定;
 + 添加相应的路由规则，并启用props传递路由的参数（id）；
 + 根据接口，请求数据，进行渲染；
 + 复用comment组件，在父组件进行注册，并以标签的形式进行引用，在引用时，为其传递一个:newsid属性，属性值为当前图片的id；
## 实现缩略图效果
 + 先加载所有图片到页面上；
 + 使用`vue-preview`插件来实现缩略图效果；
## 点击商品购买跳转到商品列表页面
 + 添加商品列表组件；
## 实现商品经典两列布局
 + 外层有一个`.goods-list`,里面的每个商品都是`.goods-item`;
 + 给外层设置display:flex,同时为了让一行只显示两个商品，为`goods-item`设置`width:49%`,
   同时，为外层添加`flex-wrap:wrap`,可以自动换行；
## 改造进入商品详情页面的路由信息
## 实现商品详情页面的卡片布局
 + 使用mui提供的card.html即可；
## 实现商品页面的轮播图效果
## 实现首页和商品详情页面的轮播图宽高设置
 + 首页和商品详情页面的宽高有分歧；
 + 首页要求宽高100%；商品详情页面要求高度100%，宽度自适应；
 + 让轮播图组件提供一个`isfull`属性，表示是撑满父元素；
 + 如果为true，宽和高都是100%；
 + 如果为false，宽度自适应，高度100%；
## 关于轮播图子组件的说明
 + 调用轮播图子组件时，需要通过属性绑定的形式，为其传递三个值，
 + `lunbotu`表示要渲染的数据；
 + `imgname`表示渲染的属性名可能不一样，每一个父组件都要指定一个要渲染的属性名；
 + `isfull`表示为按需设置轮播图的样式；


